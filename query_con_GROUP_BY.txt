1. Contare quanti iscritti ci sono stati ogni anno
SELECT YEAR(enrolment_date) AS `Anno immatricolazione`, COUNT(id) AS `Iscritti` FROM students GROUP BY YEAR(enrolment_date);

2. Contare gli insegnanti che hanno l'ufficio nello stesso edificio
SELECT office_address AS `Indirizzo`, COUNT(id) AS `Num. Professori` FROM teachers GROUP BY office_address;

3. Calcolare la media dei voti di ogni appello d'esame
SELECT exam_id AS `Id appello`, AVG(vote) AS `Media appello` FROM exam_student GROUP BY exam_id;

4. Contare quanti corsi di laurea ci sono per ogni dipartimento
SELECT department_id AS `id dipartimento`, count(id) AS `num. corsi` FROM degrees GROUP BY department_id;